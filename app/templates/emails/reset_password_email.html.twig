<!doctype html>
<html lang="en">
  <body style="font-family:Arial,sans-serif;color:#1f1f1f;">
    <h2>{{ title }}</h2>
    <p>{{ greeting }}</p>
    <p>{{ intro }}</p>
    <p><a href="{{ action_url }}">{{ cta }}</a></p>
    <p>{{ outro }}</p>
  </body>
</html>
